{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'checker service: no Next.js build needed'"
  },
  "deploy": {
    "startCommand": "npm run check",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
