{
  "name": "ops-monitor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/checker/*.test.ts src/lib/*.test.ts",
    "check": "tsx src/checker/run.ts",
    "check:local": "tsx --env-file=.env.local src/checker/run.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.1",
    "tsx": "^4.23.9"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
